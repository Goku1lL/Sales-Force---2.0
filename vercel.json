{
  "version": 2,
  "buildCommand": "npx prisma generate && pnpm install && pnpm --filter @sfa/shared build && pnpm run build",
  "devCommand": "cd apps/frontend && npx vite",
  "outputDirectory": "apps/frontend/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/api/v1/:path*", "destination": "/api/:path*" }
  ]
}
